# Copy this file to .env.local and paste your key.
# .env.local is gitignored. This key is read by vite.config.js on the dev server
# only -- it is never bundled into the browser build.
# Get a free key at https://www.pokemonpricetracker.com/api
POKEMONPRICETRACKER_API_KEY=pokeprice_your_api_key_here
